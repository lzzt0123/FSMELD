#ifndef FSMELD_DEF_H
#define FSMELD_DEF_H

#define dist(x, y) (((x) - (y)) * ((x) - (y)))
#define MAX_VAL 1008610086
#define MIN(x, y) ((x) < (y) ? (x) : (y))
#define MAX(x, y) ((x) > (y) ? (x) : (y))

#endif
